{
  "version": 2,
  "rewrites": [
    { "source": "/attestations/:path*", "destination": "/attestations/:path*" },
    { "source": "/audits/:path*", "destination": "/audits/:path*" }
  ],
  "routes": [
    { "src": "/", "dest": "/index.html" }
  ]
}
